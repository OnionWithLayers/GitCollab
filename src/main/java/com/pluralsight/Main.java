package com.pluralsight;

import java.util.ArrayList;

public class Main { //DISCLAIMER THE TSUDOCODE IS MOST DEFINITELY WRONG
    //Scanner scanner = new Scanner(System.in);
    public static void main(String[] args) {
        System.out.println("Hello World");

        ArrayList<Integer> ints = new ArrayList<>();
        ints.add(1);
        ints.add(2);
        ints.add(0);
        ints.add(9);


        /*
        Sandwich sandwich = new Sandwich;

        "welcome to sandwich shop"
        sandwich = sandwich code();


        when calculating price, make calculatingPrice method, take the 'sandwich' as an input, have the program
        maybe section off or organize the elements in the sandwich and find the match the elements inside to what
        prices are associated to what item
         */
    }

    /*
    ***sandwich code(Sandwich sandwich){
    "what size sandwich do you want? (4", 8", 12")"
    int size = scanner.next(int);
    scanner.nextline();
     "What type of bread would you like?"
     "white, wheat, rye, wrap"
     String breadType = scanner.nextLine();
     "Toppings?"
     String topping = scanner.nextLine();
     "Whats the sauce, boss"
     String sauce = scanner.nextLine();

     Sandwich sandwich = new Sandwich(size, breadType, topping, sauce);
    }
     */
}
